<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.plass.computer.pplas_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DoctorListActivity" />

        <service
            android:name=".Patient.PatientDataHandleService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".EmergencyActivity1" />
        <activity android:name=".Patient.LoginActivity2" />
        <activity android:name=".Manager.ManagerSettingActivity" />
        <activity android:name=".Manager.AddStaffActivity" />
        <activity android:name=".Manager.AddPatientActivity"></activity>
    </application>

</manifest>